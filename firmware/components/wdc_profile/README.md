# wdc_profile

Static device profile and logical resource lookup.

The R9 scaffold uses a relay-node rev-c profile with logical GPIO, sensor, and network resources. The profile is the physical/policy truth used after manifest capability checks.

Future work: generate the static C profile from the JSON profile after schema validation.
