#include "wdc_abi.h"

/*
 * R3 pointer validation lives in wdc_host_call.c so the full host-call
 * boundary can be compiled as one minimal component source for ESP-IDF.
 * This file is intentionally source-empty and kept only to preserve the
 * component path for older roadmap notes.
 */
