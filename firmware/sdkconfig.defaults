# Cross-family firmware invariants. Target and board assumptions belong in
# realization overlays selected by firmware/idf-family-matrix.json.
CONFIG_LOG_DEFAULT_LEVEL_INFO=y
CONFIG_COMPILER_OPTIMIZATION_SIZE=y
CONFIG_APP_REPRODUCIBLE_BUILD=y
